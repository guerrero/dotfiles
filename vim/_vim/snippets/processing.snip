snippet     setup_hype
abbr        hype
options     head
  void setup() {
    size(${1:640},${2:640}${3:,P3D});
    H.init(this).background(#${4:202020})${5:.use3D(true)}${6:.autoClear(${7:true\})};
    smooth();
  }

  void draw() {
    H.drawStage();
    ${8: ${9:// \}saveFrame("frames/#########.tif"); if (frameCount == 900) exit();}
  }
